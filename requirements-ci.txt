# Minimal dependencies for CI (lint + tests).
# Does NOT include torch, opencv, paddlepaddle, or other heavy CV packages —
# the test suite in tests/test_api/ and tests/test_db/ does not need them.

# API
fastapi==0.111.0
starlette==0.37.2
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Task queue (imported but not executed in tests)
celery>=5.3.6
redis>=5.0.0

# Metrics (used by test_db)
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
shapely>=2.0.0

# Config & utils
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
loguru>=0.7.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Lint
ruff>=0.4.0
