# Copy this to .env.local and fill in values

# FastAPI backend URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# The dashboard signs in instead of reading an academy from the environment.
# scripts/seed_dev.py prints the academy UUID and password to use.
